name: eslint

on: [push]

jobs:
  docker:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Start containers
      run: docker-compose -f "docker-compose-develop.yml" up -d --build

    - name: Run eslint
      run : docker-compose -f "docker-compose-develop.yml" run develop node --version