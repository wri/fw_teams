name: eslint

on: [pull_request]

jobs:
  docker:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        ref: ${{ github.head_ref }}

    - name: Start containers
      run: docker-compose -f "docker-compose-develop.yml" up -d --build

    - name: Run eslint
      run : docker-compose -f "docker-compose-develop.yml" run develop yarn run lint

    - name: Commit prettier changes
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: "auto commit: apply eslint fixes"

    - name: Stop containers
      if: always()
      run: docker-compose -f "docker-compose-develop.yml" down